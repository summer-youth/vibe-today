# 바이브투데이 (VibeToday)

> 나의 하루 생각과 느낌을 세상에 단 하나뿐인 AI 그림 일기로 만들어주는 모바일 다이어리

## 📱 프로젝트 소개

바이브투데이는 사용자의 일상을 AI가 생성한 유니크한 이미지로 기록하는 모바일 다이어리 앱입니다.

### 핵심 기능

- 🔐 **Google 소셜 로그인**: 간편한 인증으로 나만의 그림 일기장 시작
- 🎯 **오늘의 키워드**: 매일 다른 영감의 키워드 제공 (#설렘, #도전, #평온 등)
- ✍️ **텍스트 기록**: 키워드에 맞춰 자신의 하루를 간단히 작성
- 🎨 **AI 이미지 생성**: Gemini API로 세상에 단 하나뿐인 이미지 생성
- 📚 **바이브 갤러리**: 나의 지난 기록들을 갤러리 형태로 모아보기

## 🛠 기술 스택

- **Frontend**: React Native (Expo)
- **Backend**: Firebase (Authentication, Firestore, Storage)
- **AI**: Gemini API (이미지 생성)
- **State Management**: Zustand
- **Navigation**: React Navigation
- **Language**: TypeScript

## 📁 프로젝트 구조

Screaming Architecture 패턴을 따릅니다:

\`\`\`
src/
├── features/           # 기능별 모듈
│   ├── auth/          # 인증 관련
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── services/
│   │   ├── store/
│   │   └── validation/
│   ├── vibe/          # 바이브 생성 및 관리
│   └── gallery/       # 갤러리 기능
├── pages/             # 화면 컴포넌트
├── ui/                # 공통 UI 컴포넌트
├── hooks/             # 공통 커스텀 훅
├── utils/             # 유틸리티 함수
├── constants/         # 상수 정의
├── services/          # 외부 서비스 연동
└── App.tsx            # 앱 진입점
\`\`\`

## 🚀 시작하기

### 사전 요구사항

- Node.js 18 이상
- npm 또는 yarn
- Expo CLI
- Firebase 프로젝트
- Gemini API 키

### 설치

\`\`\`bash
# 의존성 설치
npm install

# 환경 변수 설정
cp .env.example .env
# .env 파일을 열어 필요한 값들을 입력하세요
\`\`\`

### 실행

\`\`\`bash
# 개발 서버 시작
npm start

# iOS 시뮬레이터
npm run ios

# Android 에뮬레이터
npm run android
\`\`\`

## 🔧 환경 설정

### Firebase 설정

1. [Firebase Console](https://console.firebase.google.com/)에서 프로젝트 생성
2. Authentication에서 Google 로그인 활성화
3. Firestore Database 생성
4. Storage 설정
5. iOS/Android 앱 등록 후 설정 파일 다운로드
   - iOS: `GoogleService-Info.plist`
   - Android: `google-services.json`

### Gemini API 설정

1. [Google AI Studio](https://makersuite.google.com/app/apikey)에서 API 키 발급
2. `.env` 파일에 API 키 추가

## 📊 데이터 모델

### Firestore Collection: `vibes`

\`\`\`typescript
{
  userId: string;        // Firebase Auth 사용자 ID
  createdAt: timestamp;  // 생성 날짜/시간
  keyword: string;       // 오늘의 키워드 (예: "#도전")
  text: string;          // 사용자가 작성한 텍스트
  imageUrl: string;      // 생성된 이미지 URL
}
\`\`\`

## 🎨 디자인 시스템

추후 추가 예정

## 📝 라이센스

Private

## 👥 개발자

- Hamzzi (@vibecoding)

---

**바이브투데이**로 당신의 하루를 특별한 그림으로 기록하세요! 🎨✨

